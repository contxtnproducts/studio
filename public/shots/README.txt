Export the marquee screenshots from Figma into this folder as
01.png ... 12.png (PNG @2x is fine).

Any file that is missing simply renders as an empty outlined tile —
the layout stays correct either way, so you can add them one at a time.
